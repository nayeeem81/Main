# Multi Tenant Implementation

## Monolithic Application and Shared Database

### Tenant Isolation:
1. Database Row Level (Global QueryFilter)
2. Identity Authenticatin (Cookie Based Encripted)
3. Authorization: Policy Based (Teanent Role)
4. Check tenant isolation (from request and session valdation)
5. For su directory based tenants, we rewrite the url (so, browser and server manage te auth cookie in default Identity mechanism.
6. For unsafe requests, Antiforgery is handled by the default process: action attribute and tag in the view.
7. For fetch and ajax, header is attached.

### Tenants:
1. Domain
2. Sub Domain
3. Sub Directory

### Tenant Resolver: 
1. Tenant resolved in Middleware uses Session & Momory Cache
2. Resolved Tenant is kept in session
3. Unresolved Tenant is searched in Database and keep in Session
4. If tenant is foundnin Session, bypass te Datbase search
5. Global Query Filter (TenantId) ) is set from here in a interface (ITenantSetter) and using DI database is partitioned for each tenant (isolation)

## Authenticaton:
1. Default Identity (Authentication)
2. Default Identity Role [GlobalAdmin and User]
3. Email is unique in Identity.
4. We keep the default identity authentication.
5. After te user is identified and pass the authentication; we create claims and make the user isolated only for the tenant he/she is authenticated.

## Authorization
### Step 1: 
1. Tenant Role Policy Based
2. Tenant Claims ( Authorization for Tenant Roles)
3. Claims:

We chech the tenantId from the Middleware set ItenantSetter (TenantId).
We use Session Key: CurrentTenantId and get the TeanantId 
We match the ItenantSetter.TenantId with the session TenantId
We find the role for this user: (Identity Role & TenantRole)
Then, we make create a claim for this user with the combinatoon of (UserId, TenantId and TenantRole). 
We set te claim for the user.

### Step 2
### Authorizatin Handeler
The handeler reads the claim combinaton from the user claim and if it is a valid claim, it assigns a policy created for the TenantRole.

### Controllers & Action: (Use Policy)
1. Controller Class: We use authorizaton policy for the tenant user 
2. Controller Action: We use authorizaton policy for the tenant user 

## Scale
1. Applicaton is monlithic. We can only create 2 to 3 more deploys (instances) in same vps and using round robin algorithm, we forward the requests the specific ports.


# Multi-Tenant Implementation

## Monolithic Application & Shared Database

## Supported Tenant Strategies
The system supports flexible tenant identification through:Domain, Subdomain, Subdirectory

## Tenant Resolution (Middleware)
### Resolution Process: 
The TenantResolver middleware identifies the tenant from the request (using routing or headers) and leverages Session and Memory Cache for quick lookups.Caching. The resolved tenant is cached in the active session. For unresolved requests, the system searches the database, stores the result in Session, and bypasses subsequent DB lookups. A global filter (based on TenantId) is set via an ITenantSetter interface. Using Dependency Injection (DI), the database is partitioned and isolated for each tenant.

## Authentication: 
Default Identity: Keeps the default ASP.NET Core Identity setup. Uses encrypted, cookie-based default ASP.NET Core Identity. Account Uniqueness: Email remains unique across the global user base.

## Authorization: 


## Authorization
### Step 1: Claim Construction
1. The middleware retrieves the TenantId from ITenantSetter and matches it with the CurrentTenantId stored in the session.The system fetches the user's combined roles (global Identity Roles + Tenant-specific Roles).
2. A unique user claim is created containing the UserId, TenantId, and TenantRole.These newly combined claims are then attached to the authenticated user's principal.

### Step 2: Authorization Handler Execution
The custom authorization handler reads the claim combination from the user's principal. If the claims are valid, it assigns the appropriate tenant-level policy authorization.

## Tenant & (Tenant User Role) Isolation
1. Policy based Tenant Role (authorization)
	Roles: Operates on the default Identity Roles (GlobalAdmin, User).
	TenantRole: Policy-based, driven by tenant-specific roles. 
	Tenant Scoping: 
	Once a user is authenticated, the system generates custom claims that isolate the user to the specific tenant they are accessing.
2. Data Partitioning: Data is partitioned at the row level via EF Core's Global Query Filters.
3. Keeping the code structure clean and memorable.
4. Subdirectory Handling: Routes are rewritten for subdirectory-based tenants, allowing the browser and server to manage authentication cookies natively. (Removes code complexity)
3. Request Security: Unsafe requests are protected by the default Antiforgery mechanisms. Fetch and AJAX requests require an explicit header attachment.