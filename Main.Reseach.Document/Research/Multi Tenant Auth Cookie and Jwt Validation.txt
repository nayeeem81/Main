To implement multi-tenant resolution and validation using auth cookies or JWTs in an ASP.NET Core application, resolve the tenant before authentication to enforce tenant-specific security parameters (like distinct secret keys).

Extracting JWT Tokens from Cookies

If your JWT is stored in an HTTP cookie, the standard JwtBearer middleware won't find it by default since it checks the Authorization header. You can configure the OnMessageReceived event to extract the JWT from the cookie.
csharp

builder.Services.AddAuthentication(options =>
{
    options.DefaultAuthenticateScheme = JwtBearerDefaults.AuthenticationScheme;
    options.DefaultChallengeScheme = JwtBearerDefaults.AuthenticationScheme;
})
.AddJwtBearer(options =>
{
    options.TokenValidationParameters = new TokenValidationParameters
    {
        ValidateIssuerSigningKey = true,
        ValidateIssuer = true,
        ValidateAudience = true,
        ValidIssuer = "https://your-identity-provider.com"
    };

    options.Events = new JwtBearerEvents
    {
        OnMessageReceived = context =>
        {
            // Extract the JWT from the specific cookie
            if (context.Request.Cookies.ContainsKey("AuthToken"))
            {
                context.Token = context.Request.Cookies["AuthToken"];
            }
            return Task.CompletedTask;
        }
    };
});

Use code with caution.

Implementing Multi-Tenant Resolver Middleware:

Create a custom middleware that parses the JWT or cookie payload to determine the tenant. This must run before app.UseAuthentication() to ensure tenant-scoped data and keys are loaded into your DI container. 

csharp

public class TenantResolverMiddleware
{
    private readonly RequestDelegate _next;

    public TenantResolverMiddleware(RequestDelegate next)
    {
        _next = next;
    }

    public async Task InvokeAsync(HttpContext context, ITenantService tenantService)
    {
        // 1. Resolve Tenant ID from JWT or Cookie
        var tenantId = GetTenantFromContext(context); 

        // 2. Set the tenant in your tenant service (or DI container)
        if (!string.IsNullOrEmpty(tenantId))
        {
            await tenantService.SetCurrentTenantAsync(tenantId);
        }

        await _next(context);
    }

    private string GetTenantFromContext(HttpContext context)
    {
        // Read from header, claim, or route (whichever applies to your JWT structure)
        // Example: parsing the tenant from a standard JWT sub or custom claim
        var tenantClaim = context.User.FindFirst("tenantId")?.Value;
        return tenantClaim;
    }
}

Use code with caution.

Tenant-Aware Token Validation:

In a multi-tenant application, one tenant's admin could potentially use their signed token to forge requests to another tenant. You must dynamically validate the IssuerSigningKey based on the resolved tenant.
csharp

options.Events = new JwtBearerEvents
{
    OnTokenValidated = async context =>
    {
        var tenantService = context.HttpContext.RequestServices.GetRequiredService<ITenantService>();
        
        // Ensure the token's tenant matches the resolved tenant ID
        var tenantIdFromToken = context.Principal.FindFirst("tenantId")?.Value;
        var currentTenantId = await tenantService.GetCurrentTenantIdAsync();

        if (tenantIdFromToken != currentTenantId)
        {
            context.Fail("Unauthorized: Invalid tenant context");
        }
    }
};

Use code with caution.

Recommended Flow in Program.cs:

Ensure the execution order correctly handles the resolver:

app.UseMiddleware<TenantResolverMiddleware>()
app.UseAuthentication()
app.UseAuthorization()

Could you tell me:

1. How your tenant ID is passed (e.g., in the URL route, JWT claims, or subdomain)?2. Where you store your tenant-specific secrets (e.g., in a database or appsettings.json)?

I can provide the exact implementation code for your tenant resolution strategy:

What do you mean by Tenant Secret? Is it the TenantId and the tenant table in database? or itis theTenantId after resolve, where are the tenantid is stored? in my case, it is stored inthe session. Later for valdation. I have a header  TenantId comes with request object. Then, I validate taking from the sessin by key.

Tenant Secrets vs Tenant IDs

A Tenant Secret is not the TenantId. 

It is a secret key (like a cryptographic signature key or a client password) used to sign and verify JWT tokens specifically for that tenant.

Tenant ID: A unique public identifier (e.g., tenant-abc-123 or a GUID).

Tenant Table: A database table that maps the TenantId to things like database connection strings or configurations.

Tenant Secret: A private string used by your authentication server to generate JWT tokens. 

If every tenant has their own secret, your API needs to look up that secret to validate the JWT.

Understanding the Session and Header Flow

In your architecture, storing the TenantId in the Session and verifying it against an incoming HTTP Header is a common stateful approach.

However, HTTP headers can be easily manipulated or faked by malicious users. The session acts as your "Source of Truth" because it is securely stored on your server.

How to Implement Your Middleware and Validation:

Here is exactly how to build your middleware using your specific workflow (Header + Session validation):

1. The Tenant Resolver Middleware

This middleware reads the X-Tenant-ID header from the request, looks up the session, and validates that they match.
csharp


public class TenantResolverMiddleware
{
    private readonly RequestDelegate _next;

    public TenantResolverMiddleware(RequestDelegate next)
    {
        _next = next;
    }

    public async Task InvokeAsync(HttpContext context)
    {
        // 1. Extract the TenantId from the request header
        if (!context.Request.Headers.TryGetValue("X-Tenant-ID", out var headerTenantId))
        {
            context.Response.StatusCode = StatusCodes.Status400BadRequest;
            await context.Response.WriteAsync("Missing Tenant ID header.");
            return;
        }

        // 2. Extract the TenantId stored inside the Server Session
        var sessionTenantId = context.Session.GetString("TenantId");

        if (string.IsNullOrEmpty(sessionTenantId))
        {
            context.Response.StatusCode = StatusCodes.Status401Unauthorized;
            await context.Response.WriteAsync("No active session found for this tenant. Please login.");
            return;
        }

        // 3. Cross-validate the Header value against the Session value
        if (headerTenantId != sessionTenantId)
        {
            context.Response.StatusCode = StatusCodes.Status403Forbidden;
            await context.Response.WriteAsync("Tenant ID mismatch. Request rejected.");
            return;
        }

        // 4. Validation passed! Store it in HttpContext for downstream usage
        context.Items["CurrentTenantId"] = sessionTenantId;

        await _next(context);
    }
}

Use code with caution.

2. Registering the Middleware Order

For ASP.NET Core sessions to work inside your middleware, you must place your custom middleware after app.UseSession() in your Program.cs file:csharp

var app = builder.Build();

app.UseRouting();

// 1. Session must be initialized first
app.UseSession(); 

// 2. Run your tenant validation while session data is available
app.UseMiddleware<TenantResolverMiddleware>(); 

app.UseAuthentication();
app.UseAuthorization();

app.MapControllers();
app.Run();

Use code with caution.

If you are also using JWT tokens alongside this setup, the validation will follow the exact same logic inside the token validation events.

If you want, I can:
1. Show you how to integrate your JWT validation into this session check
2, Show you how to access this TenantId inside your Database Context for data isolation.

Show how to integrate your JWT validation into this session check. Also, where to generate the jwt and storein browser.  Show, After I resolve the tenant by (domain, subdomain, sub directory), I get tenantId; at that point, should I ceate the jwt and  add in header? also when the user is logged in, we assignthesame one jwt, or create a new one. Or can we keep a fixed jwt tokent for each tenant. after database lookup during parseing, can we use te fixed jwt token for a  tenant? 

1. Where to Generate and Store the JWT

Where to Generate: 

The JWT is always generated on the backend server.Where to Store: Store the JWT in an HttpOnly, Secure, SameSite=Strict Cookie on the browser. Do not store it in localStorage or sessionStorage, as those are vulnerable to Cross-Site Scripting (XSS) attacks.

2. The JWT Token Strategy (Fixed vs. Dynamic)

Can you use a fixed JWT token per tenant? No, absolutely not. 

A JWT is designed for Authentication (proving who a user is) and expires quickly. If you use a single fixed JWT for an entire tenant, anyone who steals that token gets full access to that tenant's entire organization forever.

The Correct Flow:

Tenant Resolution (Public Strategy): 

When a user hits ://myapp.com or ://myapp.com, you look up the TenantId based on the domain/URL. Do not create a JWT here. At this stage, the user is still anonymous. You just set a cookie or tracking mechanism for the Tenant Context.

User Authentication (Secure JWT Creation): 

When the user explicitly fills out the login form and clicks "Login", you verify their password. If valid, you generate a brand new, user-specific JWT that contains both the UserId and the resolved TenantId inside its payload claims.

3. Integrated Flow Architecture

Here is the complete code implementation showing how to resolve the tenant by URL/Header, validate the session, and enforce cross-validation inside the JWT authentication events.

Step A: Configure JWT and Cookie Extraction

We tell ASP.NET Core to look inside our secure X-Auth-Token cookie to find the JWT during the authentication phase.
csharp// Program.cs


builder.Services.AddAuthentication(options =>
{
    options.DefaultAuthenticateScheme = JwtBearerDefaults.AuthenticationScheme;
    options.DefaultChallengeScheme = JwtBearerDefaults.AuthenticationScheme;
})
.AddJwtBearer(options =>
{
    options.TokenValidationParameters = new TokenValidationParameters
    {
        ValidateIssuerSigningKey = true,
        IssuerSigningKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes("YourSuperSecretBackEndKey1234567890!")),
        ValidateIssuer = false, // Set to true if you validate issuers
        ValidateAudience = false,
        ClockSkew = TimeSpan.Zero
    };

    options.Events = new JwtBearerEvents
    {
        OnMessageReceived = context =>
        {
            // Extract the JWT from our secure HTTP cookie automatically
            if (context.Request.Cookies.TryGetValue("X-Auth-Token", out var token))
            {
                context.Token = token;
            }
            return Task.CompletedTask;
        },
        OnTokenValidated = context =>
        {
            // Cross-validate token claims with resolved middleware data
            var httpContext = context.HttpContext;
            
            // Grab the TenantId injected by our TenantResolverMiddleware
            var resolvedTenantId = httpContext.Items["CurrentTenantId"]?.ToString();
            
            // Extract the TenantId baked inside the secure JWT
            var jwtTenantId = context.Principal?.FindFirst("tenantId")?.Value;

            if (string.IsNullOrEmpty(resolvedTenantId) || resolvedTenantId != jwtTenantId)
            {
                context.Fail("Security violation: Token tenant does not match request tenant context.");
            }

            return Task.CompletedTask;
        }
    };
});

Use code with caution.

Step B: The Multi-Strategy Tenant Resolver & Session Middleware

This middleware handles your URL parsing (subdomain/path), tracks it via your HTTP Header/Session rule, and prepares the request context.
csharp

public class TenantResolverMiddleware
{
    private readonly RequestDelegate _next;

    public TenantResolverMiddleware(RequestDelegate next)
    {
        _next = next;
    }

    public async Task InvokeAsync(HttpContext context)
    {
        string tenantId = null;

        // Strategy 1: Resolve by Subdomain/Host or Path (First time hit)
        var host = context.Request.Host.Host; // e.g., ://myapp.com
        if (host.Contains(".myapp.com"))
        {
            var subdomain = host.Split('.')[0];
            tenantId = LookUpTenantIdInDatabase(subdomain); 
        }

        // Strategy 2: If fallback needed, read from incoming Header
        if (string.IsNullOrEmpty(tenantId) && context.Request.Headers.TryGetValue("X-Tenant-ID", out var headerTenantId))
        {
            tenantId = headerTenantId;
        }

        // Strategy 3: Enforce Session validation if user is already logged in
        var sessionTenantId = context.Session.GetString("TenantId");
        if (!string.IsNullOrEmpty(sessionTenantId))
        {
            // If session exists, it must strictly match our resolved tenant
            if (!string.IsNullOrEmpty(tenantId) && tenantId != sessionTenantId)
            {
                context.Response.StatusCode = StatusCodes.Status403Forbidden;
                await context.Response.WriteAsync("Tenant context tampering detected.");
                return;
            }
            tenantId = sessionTenantId; // Fallback to session state
        }

        if (string.IsNullOrEmpty(tenantId))
        {
            context.Response.StatusCode = StatusCodes.Status400BadRequest;
            await context.Response.WriteAsync("Could not resolve valid Tenant context.");
            return;
        }

        // Save resolved TenantId to HttpContext for the downstream JWT event validation
        context.Items["CurrentTenantId"] = tenantId;

        await _next(context);
    }

    private string LookUpTenantIdInDatabase(string lookUpKey)
    {
        // Query database table mapping (domain/subdomain -> TenantId)
        // Mock example:
        return "tenant_guid_12345"; 
    }
}

Use code with caution.

Step C: The Login Endpoint (Where JWT is Born)

When a user authenticates against your database, build the JWT containing the verified TenantId, then drop it straight into an HttpOnly cookie.

csharp

[ApiController]
[Route("api/auth")]
public class AuthController : ControllerBase
{
    [HttpPost("login")]
    public IActionResult Login([FromBody] LoginModel model)
    {
        // 1. Resolve current tenant ID from middleware tracking
        var resolvedTenantId = HttpContext.Items["CurrentTenantId"]?.ToString();
        
        // 2. Validate user credentials against this specific tenant's users
        bool isValidUser = MyUserValidationService.Validate(model.Username, model.Password, resolvedTenantId);
        if (!isValidUser) return Unauthorized("Invalid credentials.");

        // 3. Generate user-specific claims including the Tenant ID
        var claims = new[]
        {
            new Claim(ClaimTypes.NameIdentifier, model.Username),
            new Claim("tenantId", resolvedTenantId) // Embedded safely inside cryptographically signed JWT
        };

        var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes("YourSuperSecretBackEndKey1234567890!"));
        var creds = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);

        var token = new JwtSecurityToken(
            claims: claims,
            expires: DateTime.UtcNow.AddHours(2),
            signingCredentials: creds
        );

        var tokenString = new JwtSecurityTokenHandler().WriteToken(token);

        // 4. Set TenantId in server Session
        HttpContext.Session.SetString("TenantId", resolvedTenantId);

        // 5. Securely send JWT token to browser via HttpOnly Cookie
        var cookieOptions = new CookieOptions
        {
            HttpOnly = true,   // Prevents Javascript from reading the token (Stops XSS)
            Secure = true,     // Forces HTTPS transmission
            SameSite = SameSiteMode.Strict, // Mitigates Cross-Site Request Forgery (CSRF)
            Expires = DateTime.UtcNow.AddHours(2)
        };
        Response.Cookies.Append("X-Auth-Token", tokenString, cookieOptions);

        return Ok(new { message = "Logged in successfully", tenant = resolvedTenantId });
    }
}

Use code with caution.

Step D: Enforce Request Execution Pipeline

Ensure Program.cs triggers elements in this exact chronological order:
csharp

app.UseRouting();

app.UseSession(); // 1. Prepares session access

app.UseMiddleware<TenantResolverMiddleware>(); // 2. Finds & locks tenant context

app.UseAuthentication(); // 3. Reads JWT cookie & fires OnTokenValidated cross-check

app.UseAuthorization(); // 4. Validates user permissions

app.MapControllers();

Use code with caution.

1. Would you like me to filter these options by complexity and show you how to securely add a CSRF token mechanism to complement this Cookie/Session approach?


Since you are storing your JWT inside a secure HttpOnly Cookie, your application is highly secure against Cross-Site Scripting (XSS). However, relying on cookies automatically exposes your backend to Cross-Site Request Forgery (CSRF) attacks, because browsers automatically attach cookies to requests originating from external malicious sites.

To completely secure your Multi-Tenant app, we will implement an Anti-Forgery (CSRF) Token pattern that works seamlessly with your Tenant ID tracking.

Step 1: The Secure CSRF Strategy

When logging in: 

The server generates a unique, random string (the CSRF token).

The Response: 
The server sends this token back to the browser in a standard, readable JavaScript Cookie (e.g., XSRF-TOKEN).

The Front-End (Angular/React/Vue): 
Your front-end app reads this cookie using JavaScript and automatically attaches its value to a custom HTTP Header (e.g., X-XSRF-TOKEN) on every subsequent API call (POST, PUT, DELETE).

The Validation: The server compares the token sent in the custom header against the secure token it knows about. 

A malicious site can't steal the readable cookie due to browser security boundaries (SameSite=Strict), blocking the attack.

Step 2: Configure Anti-Forgery Services in Program.cs

Add the default anti-forgery services and instruct ASP.NET Core to look for the CSRF token in the specific header your front-end will send.

csharp// Program.cs

builder.Services.AddAntiforgery(options =>
{
    // Rename the header to match common spa frameworks like Angular or Axios defaults
    options.HeaderName = "X-XSRF-TOKEN"; 
});

Use code with caution.

Step 3: Modify the Login Endpoint to Issue CSRF Tokens

Update your login logic to issue both the secure JWT cookie, set the Server Session, and drop the JavaScript-readable CSRF validation cookie.

csharp

[ApiController]
[Route("api/auth")]
public class AuthController : ControllerBase
{
    private readonly IAntiforgery _antiforgery;

    public AuthController(IAntiforgery antiforgery)
    {
        _antiforgery = antiforgery;
    }

    [HttpPost("login")]
    public IActionResult Login([FromBody] LoginModel model)
    {
        var resolvedTenantId = HttpContext.Items["CurrentTenantId"]?.ToString();
        
        // 1. (Auth Logic) Validate user...
        bool isValidUser = MyUserValidationService.Validate(model.Username, model.Password, resolvedTenantId);
        if (!isValidUser) return Unauthorized("Invalid credentials.");

        // 2. Generate and issue the JWT Cookie (HttpOnly)
        var tokenString = GenerateJwtToken(model.Username, resolvedTenantId);
        var jwtCookieOptions = new CookieOptions
        {
            HttpOnly = true,
            Secure = true,
            SameSite = SameSiteMode.Strict,
            Expires = DateTime.UtcNow.AddHours(2)
        };
        Response.Cookies.Append("X-Auth-Token", tokenString, jwtCookieOptions);

        // 3. Set TenantId in server Session
        HttpContext.Session.SetString("TenantId", resolvedTenantId);

        // 4. GENERATE AND ATTACH CSRF/XSRF TOKEN HERE
        var tokens = _antiforgery.GetAndStoreTokens(HttpContext);
        
        var xsrfCookieOptions = new CookieOptions
        {
            HttpOnly = false, // MUST be false so your front-end JS can read it
            Secure = true,
            SameSite = SameSiteMode.Strict,
            Expires = DateTime.UtcNow.AddHours(2)
        };
        // Drop the token into a cookie the client framework can pick up
        Response.Cookies.Append("XSRF-TOKEN", tokens.RequestToken!, xsrfCookieOptions);

        return Ok(new { message = "Authenticated with high security protection.", tenant = resolvedTenantId });
    }

    private string GenerateJwtToken(string username, string tenantId)
    {
        var claims = new[] {
            new Claim(ClaimTypes.NameIdentifier, username),
            new Claim("tenantId", tenantId)
        };
        var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes("YourSuperSecretBackEndKey1234567890!"));
        var creds = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);
        var token = new JwtSecurityToken(claims: claims, expires: DateTime.UtcNow.AddHours(2), signingCredentials: creds);
        return new JwtSecurityTokenHandler().WriteToken(token);
    }
}


Use code with caution.

Step 4: 
Enforce Global CSRF & Tenant Validation in the Pipeline

Rather than manually putting [ValidateAntiForgeryToken] decoration over every single database mutation controller, enforce it globally on all state-changing endpoints inside Program.cs.

csharp


var app = builder.Build();

app.UseRouting();
app.UseSession(); 

// 1. Resolve domain context and cross check session tenant
app.UseMiddleware<TenantResolverMiddleware>(); 

app.UseAuthentication(); 

// 2. Globally intercept requests to validate CSRF tokens
app.Use(async (context, next) =>
{
    var method = context.Request.Method;
    // Safe HTTP methods do not alter database state; bypass verification for them
    if (HttpMethods.IsPost(method) || HttpMethods.IsPut(method) || HttpMethods.IsDelete(method) || HttpMethods.IsPatch(method))
    {
        try
        {
            var antiforgery = context.RequestServices.GetRequiredService<IAntiforgery>();
            await antiforgery.ValidateRequestAsync(context);
        }
        catch (AntiforgeryValidationException)
        {
            context.Response.StatusCode = StatusCodes.Status400BadRequest;
            await context.Response.WriteAsync("Anti-forgery token validation failed.");
            return;
        }
    }
    await next();
});

app.UseAuthorization();
app.MapControllers();
app.Run();


Use code with caution.

Step 5: 
How Your Front-End Uses This Architecture

When your front-end triggers a login request, it will receive three pieces of state:

X-Auth-Token Cookie (HttpOnly, hidden from your code).XSRF-TOKEN Cookie (Accessible, containing the anti-forgery text).X-Tenant-ID header data tracker.Your Axios configuration or HTTP Interceptor should be configured to read that cookie and map the outgoing request headers automatically:

javascript// Front-End API client setup example (Axios)

import axios from 'axios';

const apiClient = axios.create({
  baseURL: 'https://myapp.com',
  withCredentials: true // Crucial: Tells browser to include the HttpOnly JWT cookie
});

// Automatically extract 'XSRF-TOKEN' cookie value and bind to header
apiClient.interceptors.request.use((config) => {
  const xsrfToken = document.cookie
    .split('; ')
    .find(row => row.startsWith('XSRF-TOKEN='))
    ?.split('=')[1];

  if (xsrfToken) {
    config.headers['X-XSRF-TOKEN'] = decodeURIComponent(xsrfToken);
  }
  
  // Track active tenant routing header 
  config.headers['X-Tenant-ID'] = 'tenant_guid_12345'; 
  
  return config;
});

Use code with caution.

If you want, tell me:

1. What front-end framework you are building this with (Angular, React, Vue, or Blazor)?
2.Do you need assistance mapping your EF Core database DbContext query filters using this resolved tenant data?

No, they are not exactly the same.

While the middleware pipeline logic stays identical, MVC Controllers handle routing, anti-forgery tokens, and sessions differently than ApiControllers.

In MVC, you do not use manual Axios/JavaScript interceptors or manual cookie appending. 

Instead, you use built-in razor engines, form data, and native server-side redirects.Here is how the implementation shifts for an MVC Web Project Controller:

1. The MVC Login Controller (State & Redirects)

In MVC, you do not return Ok(). You return an HTML View or a RedirectToAction. Additionally, you don't manually fetch IAntiforgery via dependency injection inside the login method; MVC manages token generation automatically during view rendering.

csharp

using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Http;

// Notice: No [ApiController] attribute, and inherits from 'Controller' instead of 'ControllerBase'
public class AuthController : Controller
{
    [HttpGet]
    public IActionResult Login()
    {
        return View(); // Renders the Login.cshtml HTML form
    }

    [HttpPost]
    [ValidateAntiForgeryToken] // Built-in MVC validation attribute
    public IActionResult Login(LoginViewModel model)
    {
        var resolvedTenantId = HttpContext.Items["CurrentTenantId"]?.ToString();
        
        // Validate user credentials against your tenant context
        bool isValidUser = MyUserValidationService.Validate(model.Username, model.Password, resolvedTenantId);
        if (!isValidUser)
        {
            ModelState.AddModelError("", "Invalid credentials.");
            return View(model);
        }

        // 1. Generate user-specific JWT token string
        var tokenString = GenerateJwtToken(model.Username, resolvedTenantId);

        // 2. Attach JWT token to an HttpOnly cookie
        var cookieOptions = new CookieOptions
        {
            HttpOnly = true,
            Secure = true,
            SameSite = SameSiteMode.Strict,
            Expires = DateTime.UtcNow.AddHours(2)
        };
        Response.Cookies.Append("X-Auth-Token", tokenString, cookieOptions);

        // 3. Keep TenantId in server Session
        HttpContext.Session.SetString("TenantId", resolvedTenantId);

        // 4. Redirect directly to the dashboard page instead of returning JSON data
        return RedirectToAction("Index", "Dashboard");
    }
}

Use code with caution.

2. The MVC View Layout (Handling Anti-Forgery)

In MVC, you do not need JavaScript to read cookies. You use the built-in form tag helper <form>. It injects a hidden form input called __RequestVerificationToken into the page automatically.

html@* Views/Auth/Login.cshtml *@
@model LoginViewModel

<form asp-action="Login" method="post">
    <!-- MVC automatically generates a secure hidden CSRF token input right here -->
    
    <div>
        <label>Username</label>
        <input asp-for="Username" class="form-control" />
    </div>
    <div>
        <label>Password</label>
        <input asp-for="Password" type="password" class="form-control" />
    </div>
    <button type="submit">Log In</button>
</form>


Use code with caution.

3. Modifying Program.cs for MVC Contexts

For MVC projects, you change your service registration and global validation filters. You don't need the custom middleware that reads the raw X-XSRF-TOKEN header since MVC reads the token directly out of incoming POST form data payloads.

csharp// Program.cs

// 1. Register MVC Views and Controllers instead of web APIs
builder.Services.AddControllersWithViews(options =>
{
    // Automatically apply CSRF token checks on EVERY MVC POST/PUT request globally
    options.Filters.Add(new AutoValidateAntiforgeryTokenAttribute());
});

var app = builder.Build();

app.UseStaticFiles(); // Necessary for MVC styling/js assets
app.UseRouting();
app.UseSession(); 

// Runs your URL parsing / Session matching validation logic
app.UseMiddleware<TenantResolverMiddleware>(); 

app.UseAuthentication(); // Evaluates the 'X-Auth-Token' Cookie
app.UseAuthorization();

// Setup standard MVC route patterns instead of attribute maps
app.MapControllerRoute(
    name: "default",
    pattern: "{controller=Home}/{action=Index}/{id?}");

app.Run();


Use code with caution.

4. How the Header Verification Changes

In your initial strategy, you mentioned that a TenantId header comes with the request object.

In Web APIs (ApiController), JavaScript passes this header continuously.

In MVC, standard browser links (<a> tags) and standard forms cannot inject custom HTTP headers into pages automatically.

Therefore, in an MVC project, your TenantResolverMiddleware should rely strictly on 

Strategy 1 (Subdomains/Paths) or 
Strategy 3 (Server Session storage) to locate the TenantId, rather than trying to look for an incoming X-Tenant-ID HTTP header field.

If you want, tell me:

1. Are you using subdomains (://domain.com) or subdirectories (://domain.com) for your MVC routes?
2. Do you need an example showing how to build an MVC route constraint to capture tenant names cleanly from URL

